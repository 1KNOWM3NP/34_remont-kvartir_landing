# REPLICATE_BRIEF — бриф для независимого повтора (сравнение моделей)

> Ты строишь лендинг **с нуля в пустом репозитории**. Существует другой вариант
> этого лендинга, но тебе **запрещено его искать, открывать и копировать**
> (репо, дизайн, тексты, код). Честное сравнение: одинаковая задача —
> полностью твоё исполнение. Дизайн — entirely твоя креативная свобода.

## 1. Цель и контекст

Лендинг-воронка компании «Ремонт квартир под ключ». Язык сайта — русский.
Цель — лиды (заявки на замер/звонок). Оплаты на сайте нет: с первого до
последнего блока веди посетителя так, чтобы ему было интересно и он захотел
оставить заявку.

Аудитория: владельцы квартир, которые боятся начинать ремонт (рост сметы,
срыв сроков, пропавшие бригады). Тон: конкретный, честный, без воды.

## 2. Стек (обязательный)

- Vite + React + TypeScript + Tailwind CSS v4 (`@tailwindcss/vite`).
- Запуск: `npm run dev` (http://localhost:5173), сборка: `npm run build`
  (обязана быть чистой), линтер без новых варнингов.
- Заявки: слой-абстракция `src/lib/api.ts` (сейчас localStorage + заглушка
  отправки, позже SQLite-бэкенд — формы не должны зависеть от реализации).
- Коммиты мелкие, с пушем после каждого шага.

## 3. Воронка (порядок блоков обязателен)

Hero → соцдоказательство → боль → решение/преимущества → калькулятор →
портфолио → видео → как работаем → FAQ → финальный CTA.
Модалка заявки открывается по всем CTA.

## 4. Функциональные требования по блокам (ЧТО, не КАК)

1. **Hero**: оффер «ремонт под ключ за N дней», подзаголовок-выгода,
   2 CTA (калькулятор / бесплатный замер), факты-доверие (стаж, гарантия).
2. **Соцдоказательство**: рейтинг (число + звёзды + площадка) и 2–4 отзыва
   с конкретикой (метраж, тип ремонта, срок, цена).
3. **Боль**: 3–4 страха клиента языком клиента, вывод в преимущества.
4. **Преимущества**: 4–6 пунктов, каждый — проверяемое обязательство
   (договор/смета/гарантия), + CTA.
5. **Калькулятор**: площадь (слайдер 20–150 м²) + тип ремонта
   (косметический от 6 000 ₽/м², капитальный от 12 000 ₽/м²,
   евро от 18 000 ₽/м²) + комнаты + опции-проценты (сантехника +8%,
   электрика +10%, дизайн-проект +5%) → диапазон цены ±10% + CTA
   «Получить точную смету» с передачей расчёта в заявку.
6. **Портфолио**: 3 проекта, интерактивное «до/после» (слайдер) +
   метраж, срок, итоговая цена + CTA.
7. **Видео**: блок о компании. Ролика нет — постер + play, клик ведёт
   в заявку («пришлём видео»). Место под будущий embed предусмотреть.
8. **Как работаем**: 4 шага от заявки до ключей + CTA.
9. **FAQ**: 6 вопросов, нативный `<details>` (доступность с клавиатуры).
10. **Финальный CTA**: инлайн-форма (та же, что в модалке) + телефон.
11. **Модалка**: имя + телефон, маска +7, валидация, фокус первого
    невалидного поля, экран успеха, закрытие по Escape/бэкдропу.

## 5. Копирайтинг (пишешь сам, наши тексты не прилагаются)

- Конкретика вместо «качественно и недорого» (числа, сроки, пункты договора).
- Один оффер на секцию. Сильные CTA («Получить смету», а не «Отправить»).
- Второе лицо, без первого («Получить», а не «Хочу»).
- 0 эмодзи. Русские кавычки «», неразрывные пробелы у единиц.

## 6. Анимации (типы обязательны, значения — твои)

- Плавный скролл (Lenis или аналог), якоря с учётом sticky-шапки.
- Появление блоков при скролле (подъём + fade, один раз).
- Стаггер-появление Hero при загрузке.
- Модалка: spring-появление (fade оверлея + панель снизу/scale).
- Живой фон (медленные пятна/градиент, только transform/opacity).
- Всё глушится при `prefers-reduced-motion`. Стек на твой выбор
  (Motion / GSAP + ScrollTrigger / своё). Никаких `transition: all`.

## 7. Фото (решаешь сам)

- Сколько и каких фото нужно под ТВОЙ дизайн и воронку — решаешь сам
  (hero, до/после, постер — или что-то своё).
- На старте разрешены заглушки с пометкой TODO.
- Когда понадобятся реальные фото — сам пишешь промпты для генерации
  (стиль, ракурс, свет под свой дизайн) и отдаёшь списком владельцу:
  количество + промпт на каждое. Владелец сгенерирует и положит
  в `public/photos/`.
- Требования к фото: фотореализм, палитра под дизайн, `<img>` с
  `width/height`, `loading="lazy"` (кроме hero), без текста в кадре.

## 8. Адаптация

375 / 768 / 1440 (MacBook) / 1920 (FHD) / 2560 (2K). Mobile-first,
но на широких контейнер и типографика растут (не «мелко по центру»).
Тач-зоны от 44px. Модалка на мобильном — bottom-sheet.

## 9. Невидимые требования

- Контраст текста 4.5:1 минимум. Только SVG-иконки.
- Семантика и a11y: skip-link, `aria-labelledby` на секциях,
  видимый `:focus-visible`, `aria-live` на динамической цене,
  `scroll-margin` под sticky-шапку.
- Заголовки иерархией h1→h2→h3, один h1.

## 10. Процесс (по чатам)

- Задача 0: каркас + инфра. 1: дизайн-система + Hero. 2: калькулятор.
  3: модалка + формы. 4: контентные блоки. 5: финал (копирайтинг-проход,
  дизайн-ревью, проверки, пуш).
- Один чат = одна задача. Порог ротации — ~20% остатка контекста:
  коммит + пуш + хэндовер (что сделано, hash, следующая задача).
- Проверка каждой задачи: `npm run build`, линтер, рендер всего App
  в строку (порядок H2, якоря резолвятся, 0 эмодзи) + визуал глазами
  в хостовом Chrome: десктоп + 375px, модалка, слайдер, FAQ.
